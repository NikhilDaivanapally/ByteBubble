services:
  - type: web
    name: my-ts-server
    env: node
    buildCommand: NODE_ENV=development npm install && npm run build
    startCommand: npm start
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production

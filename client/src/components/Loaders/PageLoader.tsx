import Loader from "../ui/Loader";

const PageLoader = () => {
  return (
    <div className="w-full h-full flex-center">
      <Loader customColor />
    </div>
  );
};

export default PageLoader;
